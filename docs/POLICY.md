# Security Policy

> **Status:** skeleton — filled in M5 when the full gate set is wired.

This document describes the severity thresholds used by the OPA/Rego policy gate
(`conftest`) and the process for requesting and approving finding exceptions.

## Severity threshold

*(M5: document the configured threshold and the normalisation map from scanner severity
labels to the common HIGH/MEDIUM/LOW/CRITICAL scale.)*

## Exception process

*(M5: document how to request an exception — required fields in `policy/data/exceptions.yaml`,
who approves, how long exceptions last before mandatory renewal.)*

## Scope

*(M5: which branches the required status check applies to; what happens on expired exceptions.)*
