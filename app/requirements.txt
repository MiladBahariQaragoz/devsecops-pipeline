# Runtime dependencies — pinned to exact currently-patched versions.
# Trivy fs scans this file in M3; keep clean of HIGH+ CVEs on main.
#
# To update: pip install flask==<new-version> then pip freeze > app/requirements.txt
# (inside the .venv, not globally).
#
# NOTE: The demo/failing-gates branch adds a known-vulnerable dependency
# (requests==2.19.1, CVE-2018-18074) to make the Trivy fs gate fire. Keep this list
# free of HIGH+ CVEs on main — never add a vulnerable pin here.
flask==3.1.3
werkzeug==3.1.8
click==8.4.2
itsdangerous==2.2.0
jinja2==3.1.6
markupsafe==3.0.3
blinker==1.9.0
